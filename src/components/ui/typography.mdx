import { Canvas, Controls, Meta, Source } from "@storybook/addon-docs/blocks";
import * as TypographyStories from "./typography.stories";
import * as SystemStories from "./typography.system.stories";

<Meta of={TypographyStories} />

# Typography

The `Typography` component renders text at a fixed scale (variant) and
weight, inheriting LifeSG's typography ramp. Use it instead of bare HTML
elements when you want the scale + weight to be explicit and consistent.

## Import

<Source
  language="tsx"
  code={`import { Typography } from "@/components/ui/typography";`}
/>

## System tour

These stories live in `typography.system.stories.tsx` (sidebar:
`Core/Typography/System Tour`) and mirror LifeSG's canonical "Typography
Set". They're labeled visualisations of the full ramp — useful when you
want to scan the scale rather than dial in a single example. Click "Show
code" on any canvas to see the canonical variant names.

<Canvas of={SystemStories.TypographySet} />
<Canvas of={SystemStories.Headings} />
<Canvas of={SystemStories.Body} />
<Canvas of={SystemStories.Weights} />

## API examples

### Default

<Canvas of={TypographyStories.Default} />

### Line clamp

<Canvas of={TypographyStories.LineClamp} />

### Props

<Controls of={TypographyStories.Default} />

## Scale reference

| Variant | Size / Line-height |
|---|---|
| `heading-xxl` | 30 / 38 |
| `heading-xl` | 26 / 34 |
| `heading-lg` | 24 / 32 |
| `heading-md` | 20 / 28 |
| `heading-sm` | 18 / 26 |
| `heading-xs` | 16 / 24 |
| `body-bl` | 18 / 28 |
| `body-md` | 16 / 24 |
| `body-sm` | 14 / 26 |
| `body-xs` | 12 / 20 |

## Notes

- `body-sm` uses a *spacious* 26 px line-height (LifeSG's BodySM line is
  taller than what Tailwind's `text-sm` would give you — 14 / 20 vs 14 / 26).
- `paragraph` adds bottom margin; `maxLines` enables CSS line-clamp.
