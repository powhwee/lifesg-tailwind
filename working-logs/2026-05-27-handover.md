# Handover — 2026-05-27

Supersedes the Storybook handover from `2026-05-24-storybook-handover.md`
(Phases 4 + 5 are now done). Today's working log is
`2026-05-27-storybook-phases-4-5.md`.

**State:** 28 commits ahead of `origin/main`, none pushed. Working tree
clean. Dev server (if you left mine running) on http://localhost:6006.

## TL;DR

Storybook roadmap done. Two follow-ups for next session:

1. **Port 6 more components** — upload, download, singpass-button,
   predictive-text-input, popover-inline, popover-v2 (rename current
   `popover` accordingly).
2. **Intro-content audit** — scan LifeSG's per-component MDX intros and
   port the developer-onboarding prose into ours where we're thinner.

Both detailed below.

## Next port — 6 components

LifeSG sources confirmed via their GitHub repo. Suggested batch order:

| Component | LifeSG dir | Notes |
|-----------|------------|-------|
| `file-upload` | `stories/file-upload/` | Form-category. Bring across as `upload.tsx` to match LifeSG dir naming, or keep `file-upload` for clarity — pick one. |
| `file-download` | `stories/file-download/` | Sibling to upload — same drag-drop chrome pattern likely |
| `singpass-button` | `stories/singpass-button/` | Specialised CTA with the Singpass logo. Selection-and-input/SingpassButton at LifeSG. |
| `predictive-text-input` | `stories/form/` (nested?) | Search for actual location — not at the top level of LifeSG stories. May be inside `form/predictive-text-input/`. |
| `popover-inline` | `stories/popover/` | This is LifeSG's `Overlays/PopoverInline`. We currently have `popover.tsx` — needs renaming OR our existing one already covers this. **Check existing component before porting.** |
| `popover-v2` | `stories/popover-v2/` | LifeSG's `Overlays/PopoverV2` — 10 stories at LifeSG. Bigger surface area than v1. |

### Before porting

For each component:
1. Check `node_modules/@lifesg/react-design-system/<comp>/` to confirm
   we have the published source as a reference.
2. Cross-check whether we already have `src/components/ui/<comp>.tsx`.
   The popover situation needs checking — LifeSG ships PopoverInline
   AND PopoverV2 as separate exports; ours may collapse them.
3. Follow the Phase 4 batch pattern: paired
   `<comp>.stories.tsx` + `<comp>.mdx`, verified via `storybook build`
   + a throwaway playwright probe before commit.

### Sidebar placement

Per the top-level sort in `.storybook/preview.ts`:

- file-upload, file-download, predictive-text-input → `Form/*`
- singpass-button → `Selection and input/*`
- popover-inline, popover-v2 → `Overlays/*`

Inside each top-level the alphabetical default applies.

## Intro-content audit — LifeSG vs ours

### What to compare

LifeSG's per-component MDX has a consistent intro shape. Sample from
`stories/accordion/accordion.mdx`:

```mdx
# Accordion

## Overview

Displays expandable and collapsible content panels that can be
toggled by the user.

```tsx
import { Accordion } from "@lifesg/react-design-system/accordion";
```

<Canvas of={AccordionStories.Default} />

## Smaller Accordion item titles

Specifying the type in the `Accordion.Item` produces a smaller text
size for each item's title.

<Canvas of={AccordionStories.SmallAccordionItemTitle} />
...
## Accessibility
...
## Component API
<PropsTable />
```

Recurring elements in LifeSG MDX:

1. **`## Overview`** — single sentence, "what this component does"
2. **Import** as a fenced code block right after Overview
3. **`<Canvas of={…Default} />`** before any variant prose
4. **Per-variant section** — short prose intro + canvas, prose explains
   "use this when…" / "set X if Y" / "without Z, …"
5. **Accessibility** — prose + code examples, often a dedicated
   `<Canvas of={Accessibility} />` story
6. **Component API** — `<PropsTable />` (their custom component;
   we use `<Controls of=>`)

### What ours generally has

Our MDX shape (e.g. `src/components/ui/accordion.mdx`):

- One-paragraph intro after the title (no explicit `## Overview` header)
- Import via `<Source language="tsx" code={…} />` (renders the same as
  a fenced block — different syntax, same output)
- Per-variant canvases under variant section headers — but the prose
  per variant is **often missing** (we just have the heading + canvas)
- Accessibility section — present on most but not all components;
  prose varies in depth
- Props via `<Controls of={…} />` — same idea as their PropsTable
- Notes section — implementation quirks, token references,
  cross-component pointers (LifeSG does NOT have this; it's our value-add)

### Suggested scan procedure

For each of our 53 component MDX files, compare against the
corresponding LifeSG MDX and identify:

1. **Missing per-variant prose** — we have a heading + canvas but no
   explanation of when to use that variant. Easy win — port the
   one-sentence intros LifeSG ships.
2. **Thinner overview** — our opening paragraph vs theirs.
3. **Missing accessibility recipes** — LifeSG often ships code
   examples (e.g. Accordion's `headingLevel` example). We document
   accessibility as bullet points; missing the code-example angle.

**Things to preserve** (do NOT port):

- **Our API separation** — we keep `comp.stories.tsx` + `comp.mdx`
  with embedded `<Canvas of=>`. Don't move to LifeSG's "single MDX
  with inline JSX" style.
- **Our Notes section** — implementation context that LifeSG
  doesn't ship is useful for ramping up.
- **System-tour split** for layout/typography/divider — LifeSG's
  Foundations follows a similar split; ours mirrors it correctly.
- **Foundations paired MDX + story** (post `9692a2c`) — the prose +
  panel separation works; preserve it.

### Mechanically

```bash
# Get LifeSG's MDX for a single component (no auth needed):
curl -sL "https://api.github.com/repos/LifeSG/react-design-system/contents/stories/<comp>/<comp>.mdx" \
  | python3 -c "import json,sys,base64; print(base64.b64decode(json.load(sys.stdin)['content']).decode())"

# Or list every component dir:
curl -sL "https://api.github.com/repos/LifeSG/react-design-system/contents/stories" \
  | python3 -c "import json,sys; print('\n'.join(f['name'] for f in json.load(sys.stdin) if 'name' in f))"
```

CloudFront blocks their manager UI but raw GitHub content works fine.

### Suggested cadence

Per component: 5–10 minutes to read both MDXs side-by-side and merge
useful prose. ~5 hours for all 53 — best done as a quality-pass batch
across one or two components per existing batch grouping (Form,
Content, etc.) rather than alphabetically.

Pick 3–5 representative components first (e.g. accordion, button,
input, modal-v2, calendar) — sample our gap before committing to
the full sweep.

## Conventions established this session (don't break)

| Convention | Where | Rationale |
|------------|-------|-----------|
| Paired `.stories.tsx` + `.mdx` for every component | `src/components/ui/*` | API separation; MDX is the canonical docs entry |
| `<Meta of={…Stories} />` always; no autodocs tag | every MDX | SB10 MDX + autodocs collision crashes the indexer |
| `.system.stories.tsx` for layout / typography / divider only | `src/components/ui/{comp}.system.stories.tsx` | LifeSG audit basis — see `storybook-system-tour-pattern` memory |
| Custom `parameters.docs.source.code` on system-tour stories | each system-tour story | Show-code displays the canonical API, not the demo plumbing |
| Foundation MDX uses `<Meta of={…Stories} />` + embeds visual panel via `<Canvas>` | `src/components/foundations/*.mdx` | Prose + visual panel; no swatch duplication |
| Top-level sidebar order via `storySort.order` in preview.ts | `.storybook/preview.ts` | Pinned to LifeSG canonical sequence |
| `remark-gfm` enabled in addon-docs MDX pipeline | `.storybook/main.ts` | GFM tables otherwise render as literal pipes |
| `next/font` mirrored in preview.ts decorator | `.storybook/preview.ts` | Avoids serif fallback in iframes |

## Open issues / pitfalls reinforced

- **SB10 indexer state-drift** — adding new MDX / stories while
  `storybook dev` is running often corrupts the in-memory index
  (`/index.json` returns 500). Restart is the only fix. Build is fine;
  it's just the dev-server hot-reload that breaks.
- **MDX `{...}` is JSX-expression** — wrap descriptive placeholders
  in inline backticks. Captured as `mdx-curly-braces-escape` memory.
- **MDX tables need remark-gfm** — already wired (commit `7d4d03d`).
  If you see literal `|` in a docs page, check `.storybook/main.ts`.
- **Probe scripts are throwaway** — pattern is create →
  `node .check-tmp.mjs` → delete in the same batch. Don't commit them.
  Use `playwright/test`'s `chromium` from project root so module
  resolution works.

## Resume procedure

1. `git status` → expect clean tree. `git log --oneline -3` should
   show `9692a2c`, `7d4d03d`, `8043260` at the top.
2. Optional `git push` if ready to publish.
3. Pick one of:
   - **Port a component** — follow the Phase 4 batch pattern.
     Start with the simpler ones (file-upload / file-download /
     singpass-button) before tackling popover-v2.
   - **Run the intro audit** — sample 3–5 representative components
     first; produce a delta report; decide whether to push through
     all 53.
4. If running `storybook dev`, restart any time you add new files
   (SB10 indexer quirk above).

## File references

- Working log this session: `working-logs/2026-05-27-storybook-phases-4-5.md`
- Previous handover (Phases 1–3): `working-logs/2026-05-25-storybook-phase1-3.md`
- Original Storybook roadmap (Phases marked complete): `working-logs/2026-05-24-storybook-handover.md`
- LifeSG preview.ts (source for our storySort): `https://github.com/LifeSG/react-design-system/blob/main/.storybook/preview.ts`
- LifeSG component MDX sample (audit reference): `https://github.com/LifeSG/react-design-system/blob/main/stories/accordion/accordion.mdx`
